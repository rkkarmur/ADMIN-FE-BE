import { PAGE_TITLES } from '@config/appConstants';

export default function Dashboard() {
        return (
                <div>
                        <h4>{PAGE_TITLES.DASHBOARD} Content</h4>
                        <p>This is the dashboard page for admin.</p>
                </div>
        );
}
